name: Format Acceptance Docs

on:
  push:
    branches:
      - launch/pre-cutover-readiness
    paths:
      - .github/workflows/format-acceptance-docs.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: launch/pre-cutover-readiness
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write docs/AI-HANDOFF.md docs/CURRENT-WORK.md
      - run: git rm .github/workflows/format-acceptance-docs.yml
      - name: Commit formatted files
        run: |
          git config user.name "ShelterPawtners automation"
          git config user.email "shelterpawtners@gmail.com"
          git add docs/AI-HANDOFF.md docs/CURRENT-WORK.md
          git commit -m "Format acceptance control-plane docs"
          git push origin HEAD:launch/pre-cutover-readiness
