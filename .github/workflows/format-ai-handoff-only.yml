name: Format AI Handoff Only

on:
  push:
    branches:
      - launch/pre-cutover-readiness
    paths:
      - .github/workflows/format-ai-handoff-only.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: launch/pre-cutover-readiness
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write docs/AI-HANDOFF.md
      - run: git rm .github/workflows/format-ai-handoff-only.yml
      - name: Commit formatted handoff
        run: |
          git config user.name "ShelterPawtners automation"
          git config user.email "shelterpawtners@gmail.com"
          git add docs/AI-HANDOFF.md
          git commit -m "Format AI handoff for acceptance"
          git push origin HEAD:launch/pre-cutover-readiness
