# Branch admin TODO

Two GitHub-admin operations remain after the 2026-09-11 reconciliation:

1. Retarget repository protection from obsolete `build/festival-mvp` to `main`.
2. Delete historical branch refs classified as safe to retire in `docs/BRANCH-CLEANUP-2026-09-11.md`.

The connected ChatGPT GitHub tool can inspect but cannot mutate rulesets or delete branch refs.
